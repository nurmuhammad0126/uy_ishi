// part of 'auth_bloc.dart';

// enum AuthStatus { initial, loading, error, success }

// class AuthState {
//   final AuthStatus bottonStatus;

//   AuthState({this.bottonStatus = AuthStatus.initial});

//   AuthState copyWith({AuthStatus? buttonStatus}) {
//     return AuthState(bottonStatus: buttonStatus ?? bottonStatus);
//   }
// }
